__pycache__/
*.pyc
*.pyo

# Raw and processed dataset files (downloaded per data/README.md)
data/ml-1m/*.dat
data/ml-1m/*.csv
data/amazon2014/*.csv
data/lfm2b-1mon/*.tsv
data/lfm2b-1mon/*.csv
data/lfm2b-1mon/*.zip
data/hm/*.csv
data/hm_full/*.csv
data/hm_3_month/*.csv
data/hm_1_month/*.csv
data/hm_1_month_cold/*.csv
data/hm_1_month_cold/cold_variant_report.json

# Sensitive files (IPs, keys, network topology, credentials)
Master/sensitive/

# Obsidian workspace config
.obsidian/

# W&B local logs
wandb/

# GPU monitoring logs
Master/temp/gpu_logs/*.csv

# Model checkpoints (temp/debug)
Master/temp/checkpoints/

# Claude Code local settings
.claude/settings.local.json

# Downloaded literature PDFs (~182 MB; re-fetchable, see Master/literature/feature_aware_reading_list.md)
Master/literature/papers/**/*.pdf

# Mirrored run checkpoints (~326 MB; archived on LEO5 scratch + local disk, not in git).
# The already-tracked canonical ml-1m user_item_proto checkpoint stays tracked.
Master/experiments/results/**/*.pth

# Reg-sensitivity sweep artifacts (~259 MB: checkpoints, per-variant explanations,
# figures, CSVs) — ride the Drive channel, not git. The committed ANALYSIS.md is kept.
Master/experiments/reg_sensitivity/**
!Master/experiments/reg_sensitivity/ANALYSIS.md

