---
date: 2026-08-15
time: "17:29"
phase: 4
---
# F5's R²_q latent-signal instrument — brief port notes (candidate only, use undecided)

Awareness-level note from the F5 read; **we may not use these metrics at all**. Purpose if we do: R²_q projects a target (popularity, attribute field) onto the top-q principal directions and measures **dominance, not presence** — the global-spectrum twin of the P_m neighborhood-purity question (retention vs demotion of attribute structure; [[2026-08-15_1639_pm-purity-port-groundtruth-target-discussion]]). Likely uses: host-vs-fI / ids-vs-noid contrasts of "which signal owns the leading structure," and the spectral restatement of the collapse finding.

Port amendments if ever used: (1) run on **row-normalized vectors** (direction space, per the gauge entry); (2) full-space semantic R² is a tautology for composed models (addressability) — only small-q is informative; (3) **our targets are correlated** (ml-1m genre-mass↔popularity ρ=0.672; H&M fields popularity-coupled), so raw twin R²s can be fed by one axis — the honest form is partialed/hierarchical (ΔR²(T_sem | T_pop) and vice versa); this **qualifies the P_m entry's "R² adjudicates" claim — it adjudicates only in partialed form**; (4) items are the natural entity set; for prototypes use the derived "alignment with the item-space popularity axis" instead (K too small for PCA regression). Registered-hypothesis-shaped candidate test: R²(T_pop) on feature-row-spanned directions **higher in noid than ids** (no ID home → popularity forced into the interpretable geometry; division-of-labor mechanism).

[[evaluation]] [[explanations]] [[paper-understanding]] [[phase-4]]
