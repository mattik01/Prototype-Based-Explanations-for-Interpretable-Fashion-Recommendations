% ============================================================================
% SAMPLE TEXT — NOT THESIS PROSE. Discussion material only (thesis-write
% Phase C); never paste into chapters/*.tex. LLM-usage-disclosure artifact.
% Date: 2026-08-21 · Granularity: full section 5.2 (fi-model) · v2
% Revision of v1 per Matteo's feedback (2026-08-21):
%  - Math LightFM-style (sum of feature embeddings) with ProtoMF paper
%    symbols: item t, prototypes p^t_l (L^t of them), representation t*,
%    free user u ∈ R^{L^t}, sim = shifted cosine, I-score(u,t).
%  - Section answers ONLY: (1) what differs from base I-ProtoMF,
%    (2) how precisely the design grounds prototypes per 5.1's definition.
%    v1's inventory/claim-boundary and cold paragraphs CUT.
%  - Mechanism of attribute values -> learned words explained with a
%    t-shirt example.
%  - Formal introduction as fI-ProtoMF + one naming sentence; ID part of
%    the justification deferred by reference to 5.4/5.5.
%  - Optional ID row highlighted with an inline \paragraph headline
%    (keystone reduction + arms live there).
%  - Shorter overall (4 paragraphs vs v1's 5 longer ones).
% Claims to re-verify at writing time: keystone "bit-identically"
% (test-pinned); no numbers quoted (two-tier policy).
% ============================================================================

% \section{The Model: Feature-Composed Item Factors}

\input{fi-model_v2_body}
