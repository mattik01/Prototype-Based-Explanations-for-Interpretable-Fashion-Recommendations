% ============================================================================
% SAMPLE TEXT — NOT THESIS PROSE. Discussion material only (thesis-write
% Phase C); never paste into chapters/*.tex. LLM-usage-disclosure artifact.
% Date: 2026-08-21 · Granularity: full section 5.2 (fi-model)
% Outline followed: the three content blocks recorded in 06_fi_protomf.tex
% §fi-model (construction/keystone/arms · terminology home · inventory),
% rendered as five paragraphs. NOTE: written to the model AS BUILT (I-host,
% per sc01 results memo §1) — the block comment's UI-host four-component
% inventory (t*/û/t̂/u*) is outdated for dc01 and was NOT followed; on the
% I host the item side of the score is fully grounded and the user vector
% fully latent, so the Block-I/Block-U score-share rule has no ungrounded
% item half to disclose. Flagged for Matteo in the session summary.
% Cite keys: melchiorre2022protomf, kula2015lightfm, chen2012svdfeature
% added as stubs to bibliography.bib this session.
% Claims to re-verify at writing time: keystone reduction "bit-identically"
% (test-pinned, implementation property — keep the test alive); no numbers
% quoted anywhere (two-tier policy).
% ============================================================================

% \section{The Model: Feature-Composed Item Factors}

\input{fi-model_v1_body}
